- 해싱이란?
해싱은 찾을 값을 다른 값들과 비교하는 것이 아님.
킷값에 산술적인 연산을 적용하여 레코드가 저장되어야 할 위치(주소)를 계산하는 것.
삽입/삭제/탐색은 이 주소에서 이루어짐
이상적인 해싱에서 삽입/삭제/탐색 연산의 복잡도는 O(1)

해싱에서 탐색은 테이블에 있는 레코드를 하나하나 비교하는 것이 아니라
탐색키로부터 레코드가 있어야 할 위치를 계산하고,
그 위치에 레코드가 있는지를 확인만 하면 된다.

해시 함수: 탐색키를 입력 받아 해시 주소 생성
해시 테이블: 함수에 의해 계산된 위치에 레코드를 저장한 테이블
            M개의 버킷을 가짐. 버킷은 s개의 슬롯을 가짐.
충돌: 서로 다른 키가 해시 함수에 의해 같은 주소로 계산되는 상황
오버플로: 충돌이 버킷의 슬롯 수보다 많이 발생하는 상황

충돌 발생 시 하나의 버킷 내 다른 슬롯에 저장하면 됨.
그런데 오버플로가 나타나면, 이를 해결해야 함.

좋은 해시 함수는
1. 충돌 적어야 함.
2. 주소가 테이블에서 고르게 분포해야 함.
3. 계산이 빨라야 함.